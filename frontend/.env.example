# Browser API path (use /api/v1 in dev so Vite proxies to the backend)
VITE_API_BASE_URL=/api/v1
# Django dev server — must be running: python manage.py runserver 0.0.0.0:8000
VITE_DEV_API_PROXY=http://localhost:8000
VITE_APP_NAME=Apex Hub
VITE_APP_TAGLINE=The Easy Way